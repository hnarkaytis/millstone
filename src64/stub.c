#ifdef HAVE_CONFIG_H
#include <config.h>
#endif /* HAVE_CONFIG_H */
#include <millstone.h> /* status_t, config_t */

status_t run_server (config_t * config) { return (ST_FAILURE); }
status_t run_client (config_t * config) { return (ST_FAILURE); }
